apath=$(pwd)
jf=$(find $apath -type f -iname '*.json')
for j in $jf; do
    nj=$(basename "$j")
    echo "processing $nj"
    #comnd="mongoimport --db graphs --collection pg --file $j --batchSize 1"
    comnd="mongofiles -d graphs put $jf"
    $comnd
    sleep 1
done
